/**
 * Functor typeclass implementing map.
 *
 * @module functor
 */

/**
 * To lift a function into a context.
 *
 * @note fmap :: Functor f => (a -> b) -> f a -> f b
 */
export type { Map } from '$/utils/adt/maybe.ts'
